# 🔄 更改默认分支并删除 master 分支

## 步骤 1：在 GitHub 上更改默认分支

1. 访问仓库设置页面：
   ```
   https://github.com/sty891459799/Dice-Treasure-Game/settings/branches
   ```

2. 在 "Default branch"（默认分支）部分：
   - 点击分支名称右侧的 **切换图标**（两个箭头）
   - 选择 `main` 分支
   - 点击 **Update**（更新）
   - 确认更改

## 步骤 2：删除远程 master 分支

在 GitHub 上更改默认分支后，运行以下命令：

```bash
git push origin --delete master
```

## 步骤 3：验证

检查远程分支，确认 master 已删除：

```bash
git ls-remote --heads origin
```

应该只看到 `main` 和 `feature/multiplayer-mode` 分支。

---

**注意**：必须先完成步骤 1（在 GitHub 上更改默认分支），否则无法删除 master 分支。

